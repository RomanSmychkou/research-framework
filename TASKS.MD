вопрос куда девать корреляцию между признаками — её надо хотя бы пометить на повестку дня

дальше сделать спецификации на инструментарий on-hands разметки purge/emargo с логированием для отслеживания утечек

потом бизнес-вьюхи на издержки
___
проект не игрушка в основном 
не понимаю где мой самый большой прокол на данном этапе
leakages принимаю и работаю с invariance логикой относительно них
ml льётся, rolling есть expanding сделаю
метрики аж непрерывные (сильно)
планы на будущее есть
разве что не оркестрировано в один цикл на монету — пол дня дела и +100 к презентабельности
но я ищу другие проколы


ну вот funding rate выглядит как кандидат на причёску
оркестрацию сделать
анализ хвостовых — хорошо но можно отложить 
fillrate — хорошо но можно отложить
___

Вопросы:
    1.
    таргеты ведь вырезаются по purge
    меня больше волнует автокорреляция чанков: как понять что твой gap достаточен. 
    мне кажется опираться от горизонта прогнозирования или фич 

    если они дают высокую автокорреляцию то выборки должны быть желательно вообще другого режима. сводиться к ЦПТ и искать какой процент данных вероятно репрезентирует с 99%+  вероятностью валидацию стратегии?

__
Что осталось сделать до первых торгов:
• метрики фич реализовать
• бизнес-метрики на глупые фичи
• сделать больше фич
• добавить L2
• перевести базу для ML на duck/parquet
• подготовить схему ML стекинга
• подготовить схему ML оптимизации порогов 
• expanding window сделать (разметку, сейчас только rolling)
_______

Текущие основные задачи:
• найти все классы и методы что не используются сейчас в проекте за исключением examples и legacy
• закончить спецификацию инструмента для замеры метрик фич
• утвердить формат записи результатов метрик и создать таблицу к ним
• сделать таблицу где будут записаны уже вычисленные по текущему контракту признаки — отказ от неэффективных вычислений
• Ввести жёсткое двойное условие по timestamp in chunk AND timestamp in (start, end) для расчёта признаков и таргетов.
——
• tools or logic: предусмотреть lookback as leakage; look-ahead by target safety;
__

Хорошо закончить:
• отрефакторить .\src\quant_system\feature_contracts.py
• Воспроизведение OrderBook Snapshots для составления признаков: 
    — ./src/local_ob/Tasks.md
    — Оптимизация издержек IO, батчинга и хранения в базе данных
    (когда будет закрыт предыдущий пункт)

• Удаление enviroment переменных и смотр за уходом от legacy patterns в скриптах

• держать funding rate полезно даже на спотовой стратегии 
— «как вы учитываете funding rate?»
— «у меня синтетическая аппроксимация и последний известный official FR в фичах одновременно»

__


Закрытие цикла:
• Замер статистичесских метрик фич (почти сделано)
• Замер непрерывных бизнес-метрик (нужна вьюха и логика расчёта pnl после вычета funding rate)

__

Отложенно:
• ml predict as column — он сильно усложняет работу
• адаптивные (по горизонту) таргеты — усложняют работу с подобием сохранения iid